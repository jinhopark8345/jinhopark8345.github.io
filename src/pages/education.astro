---
import Layout from "~/layouts/Layout.astro";
import Education from "~/components/Education.astro";
import { resume } from "~/data/resume";
---
<Layout title={`Education — ${resume.name}`} description={resume.description} sidebar={true}>
  <Education />
</Layout>
