---
import Layout from "~/layouts/Layout.astro";
import About from "~/components/About.astro";
import { resume } from "~/data/resume";
---
<Layout title={`About — ${resume.name}`} description={resume.description} sidebar={true}>
  <About />
</Layout>
