---
import Layout from "~/layouts/Layout.astro";
import Contact from "~/components/Contact.astro";
import { resume } from "~/data/resume";
---
<Layout title={`Contact — ${resume.name}`} description={resume.description}>
  <Contact />
</Layout>
